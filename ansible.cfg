[defaults]
# When running from the repo root, use the production inventory by default.
inventory = ansible/inventory/production/hosts.ini
interpreter_python = auto_silent
retry_files_enabled = False

# Keep output readable; remove if you prefer default.
stdout_callback = default
